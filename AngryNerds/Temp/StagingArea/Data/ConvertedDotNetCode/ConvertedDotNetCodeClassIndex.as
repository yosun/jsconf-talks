package {
	public class ConvertedDotNetCodeClassIndex {
		import global.01_RigidbodyGravityBounce2D;
		import global.02_OnGUIButtons;
		import global.03_Events;
		import global.04_RaycastHitTraverse;
		import global.05_RaycastHitInstantiate;
		import global.06_Collisions;
		import global.07_StaticScore;
		import global.HeavyBull;
		import global.RandColors;
		import global.ScoreKeeper;
		import global.RealBillboard;
		import global.fps;
		import global.getsize;
		import global.EachEntity;
		import global.Actions;
		import global.GraphicsMan;
		import global.WinState;
		import global.ScoreDaemon;
		import global.TheController;
		
		public static function InitializeClasses(verbose:Boolean):int{
			var c:Class;
			if (verbose) trace('Initializing global.01_RigidbodyGravityBounce2D');
			c = global.01_RigidbodyGravityBounce2D;
			if (verbose) trace('Initializing global.02_OnGUIButtons');
			c = global.02_OnGUIButtons;
			if (verbose) trace('Initializing global.03_Events');
			c = global.03_Events;
			if (verbose) trace('Initializing global.04_RaycastHitTraverse');
			c = global.04_RaycastHitTraverse;
			if (verbose) trace('Initializing global.05_RaycastHitInstantiate');
			c = global.05_RaycastHitInstantiate;
			if (verbose) trace('Initializing global.06_Collisions');
			c = global.06_Collisions;
			if (verbose) trace('Initializing global.07_StaticScore');
			c = global.07_StaticScore;
			if (verbose) trace('Initializing global.HeavyBull');
			c = global.HeavyBull;
			if (verbose) trace('Initializing global.RandColors');
			c = global.RandColors;
			if (verbose) trace('Initializing global.ScoreKeeper');
			c = global.ScoreKeeper;
			if (verbose) trace('Initializing global.RealBillboard');
			c = global.RealBillboard;
			if (verbose) trace('Initializing global.fps');
			c = global.fps;
			if (verbose) trace('Initializing global.getsize');
			c = global.getsize;
			if (verbose) trace('Initializing global.EachEntity');
			c = global.EachEntity;
			if (verbose) trace('Initializing global.Actions');
			c = global.Actions;
			if (verbose) trace('Initializing global.GraphicsMan');
			c = global.GraphicsMan;
			if (verbose) trace('Initializing global.WinState');
			c = global.WinState;
			if (verbose) trace('Initializing global.ScoreDaemon');
			c = global.ScoreDaemon;
			if (verbose) trace('Initializing global.TheController');
			c = global.TheController;
			return 44;
		}
	}
}
